import 'package:flutter/material.dart';

class PageModel {
  final String title;
  final String content;
  final Color color;

  const PageModel({
    required this.title,
    required this.content,
    required this.color,
  });
}